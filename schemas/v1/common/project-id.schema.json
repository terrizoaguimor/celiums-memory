{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://celiums.ai/schemas/v1/common/project-id.schema.json",
  "title": "project_id",
  "description": "Project scope identifier. Special values: 'global' (cross-project user memories), 'all' (admin scope only — requires admin:cross_project, P0-A enforced). Regular project ids are filesystem-derived slugs.",
  "type": "string",
  "pattern": "^(all|global|[A-Za-z0-9_./\\-]{1,256})$",
  "maxLength": 256
}
