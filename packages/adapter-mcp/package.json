{
  "name": "@celiums-memory/adapter-mcp",
  "version": "0.1.0",
  "description": "MCP adapter — remember, recall, forget, context tools for Claude Code and Cursor",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": { "build": "tsc", "test": "vitest run", "clean": "rm -rf dist" },
  "dependencies": {
    "@celiums/memory": "workspace:*",
    "@celiums/memory-types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": { "typescript": "^5.7.0", "vitest": "^4.1.5" }
}
