{
  "name": "@celiums/memory-bench",
  "version": "2.0.0",
  "private": true,
  "description": "Memory benchmark harness — LongMemEval + LoCoMo against celiums-memory, with affective/circadian ablation arms. Runs in-VPC as a k8s Job. Judge-agnostic core; dual judge (official GPT-4o-class + OSS gpt-oss-120b).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "celiums-bench": "dist/bin/run.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/bin/run.js"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.9.3"
  }
}
